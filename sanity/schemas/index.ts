import { survey } from './survey';
import { section } from './section';
import { question } from './question';
import { category } from './category';
import { scale } from './scale';

export const schemaTypes = [survey, section, question, category, scale];
